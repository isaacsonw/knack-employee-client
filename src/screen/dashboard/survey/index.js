/** @format */
import SurveyComponent from '../../../components/survey/index';
import { SurveyStyles } from './surveyStyle';

const Survey = () => {
  return (
    <SurveyStyles>
      <SurveyComponent />
    </SurveyStyles>
  );
};

export default Survey;
